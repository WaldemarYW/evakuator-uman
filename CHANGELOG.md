# Changelog

## 2024-12-28
- Compressed and added responsive WebP images with `srcset`/`sizes`.
- Swapped hero background to a responsive `<picture>` element (mobile + desktop variants).
- Cleaned CSP (removed inline allowance) and moved scripts to `scripts/main.js`.
- Fixed heading order for accessibility and added `hreflang` links.
- Added `robots.txt` and optional `_headers` for CSP on compatible hosts.
